{
  "name": "park-well",
  "version": "1.0.0",
  "description": "api implementation for a car parking management app",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "test": "yarn clean && yarn test:cover",
    "test:cover": "NODE_ENV=test nyc --reporter=text-lcov mocha --config .mocharc.js src/**/**/*.test.js models/**/*.test.js | coveralls",
    "test:dev": "yarn clean && NODE_ENV=test nyc mocha --config .mocharc.js models/**/*.test.js src/**/**/*test.js --watch",
    "migrate:db": "NODE_ENV=test sequelize db:migrate",
    "drop:db": "NODE_ENV=test psql -c 'drop database if exists park_well_test;' -U oluwafemiakinwa",
    "create:db": "NODE_ENV=test psql -c 'create database park_well_test;' -U oluwafemiakinwa",
    "seed:db": "NODE_ENV=test sequelize db:seed:all",
    "clean": "yarn drop:db && yarn create:db && yarn migrate:db && yarn seed:db"
  },
  "repository": "https://github.com/KaiserPhemi/park-well.git",
  "author": "KaiserPhemi <oluwafemi.akinwa@andela.com>",
  "license": "MIT",
  "dependencies": {
    "@hapi/joi": "^15.0.3",
    "bcrypt": "^3.0.6",
    "dotenv": "^8.0.0",
    "express": "^4.17.0",
    "jsonwebtoken": "^8.5.1",
    "nodemon": "^1.19.0",
    "pg": "^7.11.0",
    "pg-hstore": "^2.3.2",
    "sequelize": "^5.8.6",
    "sequelize-cli": "^5.4.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "coveralls": "^3.0.3",
    "mocha": "^6.1.4",
    "mocha-lcov-reporter": "^1.3.0",
    "nyc": "^14.1.1",
    "supertest": "^4.0.2"
  }
}
